@model PTrainingTrainerAtleteViewModel
@if (User.IsInRole("Trainer"))
{
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Editar Treino</h2>
            <hr />
        </div>
        <form method="post" enctype="multipart/form-data">
            <input asp-for="PersonalizedTraining.Id" hidden/>
            <div class="mb-3 row p-1">
                <div class="col-6">
                    <label asp-for="PersonalizedTraining.TrainingName" class="p-0"></label>
                    <input asp-for="PersonalizedTraining.TrainingName" class="form-control" />
                    <span asp-validation-for="PersonalizedTraining.TrainingName" class="text-danger"></span>
                </div>
                <div class="col-6">
                    <label asp-for="PersonalizedTraining.TrainingObjective" class="p-0"></label>
                    <input asp-for="PersonalizedTraining.TrainingObjective" class="form-control">
                    <span asp-validation-for="PersonalizedTraining.TrainingObjective" class="text-danger"></span>
                </div>
                <div class="mb-3 row p-1">
                    <div class="col-6 mt-4">
                        <select asp-for="PersonalizedTraining.TrainerId" asp-items="@Model.TrainerList" class="form-control">
                            <option value="" disabled selected>-- Selecionar Opção --</option>
                        </select>
                    </div>
                    <div class="col-6 mt-4">
                        <select asp-for="PersonalizedTraining.AtleteId" asp-items="@Model.AtleteList" class="form-control">
                            <option value="" disabled selected>-- Selecionar Opção --</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="mb-3 row p-1">
            </div>
            @* ********************* Botões Save e Cancel **************************** *@

            <div class="row p-2">
                <div class="col-6 col-md-3 p-1">
                    <button type="submit" class="btn btn-primary form-control">Guardar</button>
                </div>
                <div class="col-6 col-md-3 p-1">
                    <a asp-controller="Classes" asp-action="Index" class="btn btn-outline-secondary form-control">
                        Voltar
                    </a>
                </div>
            </div>
        </form>
    </div>
    @section Scripts {
@*     <script>
        tinymce.init({
        selector: 'textarea',
        height: 300,
        plugins: [
        'advlist', 'autolink', 'lists', 'link', 'image', 'charmap', 'preview',
        'anchor', 'searchreplace', 'visualblocks', 'code', 'fullscreen',
        'insertdatetime', 'media', 'table', 'help', 'wordcount'
        ],
        toolbar: 'undo redo | blocks | ' +
        'bold italic backcolor | alignleft aligncenter ' +
        'alignright alignjustify | bullist numlist outdent indent | ' +
        'removeformat | help',
        content_style: 'body { font-family:Helvetica,Arial,sans-serif; font-size:16px }'
        });
    </script> *@
    @{
            <partial name="_ValidationScriptsPartial" />
        }
}
}