@using GymTastic.Models.Models
@if (User.IsInRole("Admin"))
{
    <div class="container">
        <div class="row pt-4 pb-3">
            <div class="col-md-6">
                <h2 class="text-primary">Lista de Treinadores</h2>
            </div>@* 
            <div class="col-md-6 text-end mb-2">
                <a asp-controller="Trainer" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Adicionar Treinador
                </a>
            </div> *@
            <div class="col-md-6 text-end mb-2">
                <a asp-controller="Trainer" asp-action="Specialities" class="btn btn-secondary">
                    <i class="bi bi-list-ul"></i> Especialidades
                </a>
            </div>

        </div>
        <div class="table-responsive">
            <table id="tblData" class="table table-bordered table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>Nome do Treinador</th>
                        <th>Telefone</th>
                        <th>Email</th>
                        <th>Especialidade</th>
                        <th>TPTD</th>
                        <th>Ações</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
    @section Scripts {
        <script src="~/js/trainer.js"></script>
    }
}