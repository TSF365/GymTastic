@{
    ViewData["Title"] = "Preferências";
}

@if (User.IsInRole("Admin"))
{
    <div class="container">
        <div class="row pt-4 pb-3">
            <div class="col-md-6">
                <h2 class="text-primary">Preferências</h2>
            </div>
            <div class="col-md-6 text-end">
                <a asp-action="CreatePreference" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Nova Preferência
                </a>
            </div>
        </div>

        <div class="table-responsive">
            <table id="tblPreferences" class="table table-bordered table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Ações</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>

    @section Scripts {
        <script src="~/js/preferences.js"></script>
    }
}
